# Font Awesome Pro is served from a private registry. The auth token is read
# from the FONTAWESOME_PACKAGE_TOKEN environment variable (loaded from .env by
# mise) so no secret is ever committed. See docs/control_surface.md.
@fortawesome:registry=https://npm.fontawesome.com/
//npm.fontawesome.com/:_authToken=${FONTAWESOME_PACKAGE_TOKEN}
